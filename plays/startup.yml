---

- name: "Start cluster"
  become: no
  hosts: localhost
  connection: local
  tasks:
    - name: "Shutdown all VMs"
      virt:
        name: "{{ vm.name }}"
        state: running
      with_items: "{{ nodes.instances }}"
      loop_control:
        loop_var: vm
